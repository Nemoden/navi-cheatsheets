% kubernetes, k8s

# Set (replace) new image for deployment pod
kubectl set image deployment/<deployment> <container-name>=<image>

$ deployment: kubectl get deployments | awk 'FNR > 1 {print $1}'
$ image: docker images --filter dangling=false | awk 'FNR > 1 {print $1":"$2}'
